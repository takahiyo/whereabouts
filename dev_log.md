# 開発ログ (dev_log.md)

## 2026-04-16: ハイブリッド認証システムとパスワード管理機能の刷新

### 成功の境界線
- [x] Cloudflare Worker に `setUserPassword` アクションを新設し、管理者権限（Owner/OfficeAdmin/SuperAdmin）による検証と、厳格なパスワードバリデーションを実装。
- [x] `index.html` の管理パネルおよび拠点作成 UI から、非推奨となった `admin_password` 直接入力を削除。
- [x] 一般利用者（共有）パスワードの設定・変更時に「12文字以上、2種類以上の文字種」の強度チェックをクライアント・サーバー両面で強制。
- [x] 拠点作成（`createOffice`）時に `admin_password` が未指定の場合、自動的に一般パスワードがセットされるよう Worker 側のフォールバックを実装。
- [x] ログイン成功時の `sessionStorage` へのガードフラグ（`auth_lock` 相当）による、非同期な状態監視（`watchAuthState`）からの意図しない画面遷移の防止を確認。

### 失敗の事象
- なし。

### 失敗の根本原因
- なし。

### 次のアプローチ
- 完全に Firebase 移行が完了した後の `admin_password` カラム自体の完全なドロップ（SQL 移行）を将来的に検討。

## 2026-04-16: ローカル変更の破棄とクラウド同期の実行

### 成功の境界線
- [x] `whereabouts` ディレクトリ内の全ローカル変更を破棄。
- [x] `origin/dev` ブランチの状態に強制リセット（Hard Reset）を完了。
- [x] 未追跡ファイル（Untracked files）のクリーンアップを完了。
- [x] `git status` により、リポジトリがクリーンかつ `origin/dev` と一致していることを確認。

### 失敗の事象
- なし。

### 失敗の根本原因
- なし。

### 次のアプローチ
- 最新のクラウドの状態から、新たな開発・改修作業を再開する。

## 2026-04-15: LLM向け入口ドキュメント（LLM_CONTEXT.md）の新規作成

### 成功の境界線
- プロジェクト直下に `LLM_CONTEXT.md` を追加し、エージェント向けの**入口・実ディレクトリマップ・必読ドキュメントへのリンク・EXPORT_FOR_LLM.md の役割区分**を1か所に整理した。
- 既存の `INDEX.md` / `CORE_PRINCIPLES.md` / `docs/SYSTEM_ARCHITECTURE.md` 等を正とし、重複する詳細ルールは本ファイルではリンクに留めた。

### 失敗の事象
- なし（ドキュメント追加のみ）。

### 失敗の根本原因
- 該当なし。

### 次のアプローチ
- 認証方式やデプロイ手順の事実が変わったら、`LLM_CONTEXT.md` の「技術スタック」「ディレクトリマップ」を追随更新する。

## 2026-04-15: NotebookLM向けに LLM_CONTEXT.md へ全ソース全文を埋め込み

### 成功の境界線
- `LLM_CONTEXT.md` の後半に、`index.html` / `styles.css` / `print-list.css` / `schema.sql` / `CloudflareWorkers_worker.js` / `sw.js` / `js/**` 主要スクリプト / `main.js` / `package.json` / `wrangler.toml` の**全文**を、見出し＋フェンス付きコードブロックで連結した（`archive/` は除外）。
- 文字化け回避のため、日本語ヘッダーは `scripts/LLM_CONTEXT_header.md`（UTF-8）をテンプレートとして読み込み、`{{FILE_LIST}}` を差し替える方式にした。
- 同じ結合ルールを `scripts/build_llm_context.ps1` と `scripts/build_llm_context.mjs` の両方で再現可能にした。

### 失敗の事象
- 初回、PowerShell の here-string 内の日本語が実行環境の解釈で壊れた。続けて `ReadAllBytes`＋`Resolve-Path` 組み合わせでパス例外・null が発生した。

### 失敗の根本原因
- スクリプト本体の文字コードと PowerShell 5.1 の既定解釈の不一致（here-string）。`Resolve-Path` が特定環境で「不正なパス文字」例外を返した。

### 次のアプローチ
- ヘッダーは**外部UTF-8テンプレート**＋`ReadAllText(..., UTF8Encoding(false))` に統一。再生成は `scripts/build_llm_context.ps1` を正とする。

## 2026-03-31: 拡張カラム（伸びる列）による他カラム圧縮問題の修正

### 成功の境界線
- 備考等の「最大幅の制約がないカラム」が存在する場合において、指定幅が設定されている他のカラム（氏名、業務時間等）が本来の指定幅を維持できるようになり、画面全体のレイアウト崩れが解消された。

### 失敗の事象
- 以前の修正で、最大幅を持たない伸縮カラムに対して強制的に `width: 100%` を付与していた。
- `.panel table` 自体が `width: 100%` を持っている状態で、単一の `td/th` に `100%` の幅を要求すると、`table-layout: auto` のレンダリングアルゴリズムがその要求を最優先し、固定幅指定の他カラムを強制的に最小幅（`min-width` や内容の最小幅）まで圧縮（squish）してしまうという甚大なレイアウト崩れが発生した。

### 失敗の根本原因
- 標準のHTMLテーブルにおいて、残りの余白を埋めさせる目的で `width: 100%` を使用したこと。フレックスボックス（Flexbox）とは異なり、テーブルのセルにおける `100%` は「可能な限り親要素と同じ幅を要求する」という強欲な指定となるため。

### 次のアプローチ（今回の解決策）
- `width: 100%` の指定を廃止し、未指定の列と同様に `width: 'auto'` を指定するように緩和する。
- テーブル全体が `width: 100%` を持っているため、固定幅を持たない `auto` カラムは、他の固定幅カラムの描画後に「自然に残ったスペースをすべて吸収する」という正しいテーブルレイアウトの挙動となる。

## 2026-03-31: 幅制約の強制適用（100% vs px ロジック）

### 成功の境界線
- `table-layout: auto` において、制限したい列に具体的数値を、伸ばしたい列に `width: 100%` を与えることで、ブラウザ側のレイアウト計算をユーザーの意図通りに誘導することに成功した。
- `getEventColorMap` 等の例外処理により、DBテーブル不在時でも 500 エラーで停止しない堅牢な構成を実現した。

### 失敗の事象（以前の試行）
- 単に `max-width` を指定しても、`width: auto` 状態では内容（テキスト）の長さに押されて無視されてしまっていた。
## 2026-03-31: 500エラー解消と幅制約 (max-width) 反映の不具合修正

### 成功の境界線
- [x] `js/board.js` の修正（ストレッチ列 `100%` と制限列 `px` の明示的指定）
- [x] `CloudflareWorkers_worker.js` の修正（`getEventColorMap` 等の `try-catch` 追加）
- [x] Browser Agent による動作検証（業務時間が 50px 程度まで縮小されるか）
- [x] 開発ログ (`dev_log.md`) の更新
- `styles.css` から `table-layout: fixed` を除去し、`table-layout: auto` に変更することで、カラムへの `max-width` 指定が有効に機能するようになった。
- `CloudflareWorkers_worker.js` の `setColumnConfig` を `try-catch` で保護し、エラー時に 500 エラーではなく詳細なバックエンド情報を返却できるようになった。
- `js/board.js` の `applyWidthStyle` を `auto` レイアウト環境下で最適に動作するように調整（固定幅以外は `width: auto` + `min/max-width`）。

### 失敗の事象（解決済み）
- カラムの最大幅を 50 に設定しても画面が変化しない → `fixed` レイアウトが `max-width` を無視していたことが原因。
- 拠点設定の保存時に 500 エラーが発生 → Worker側の例外処理不足により、テーブル不在等のエラーでクラッシュしていた。

### 失敗の根本原因
- 従来の `table-layout: fixed` 設定は均等割り付けには向いていたが、今回導入した「柔軟な最小・最大幅指定 (Requirement 3)」とは技術的に相反するものであった。
- Worker の `setColumnConfig` ハンドラが DB 操作に対してフェイルセーフな設計になっていなかった。

### 次のアプローチ
- カラム幅を厳格に制御したい場合は `min == max` を指定し、内容に応じて変動させたい場合は `auto` レイアウトの特性（`min/max-width`）を最大限活用するようにユーザーに案内する。
## 2026-03-31: カラム構成UIの統合とSSOT化

### 成功の境界線
- カラム構成の管理UI（ボード表示、ポップアップ表示、並び順、最小幅・最大幅）を1つの項目リストに完全に統合できた。
- `styles.css` から幅に関するハードコーディングされた変数（`--w-name` など）を撤廃し、`js/constants/column-definitions.js` をマスター（SSOT）とする一元管理化に成功した。
- 最大幅を空（未設定/自動）にした際にメインボード上で該当カラムが画面幅に合わせて自動調整されることを確認した。
- カラムヘッダーへの説明ツールチップ（`title`属性）の動的付与が正常に動作している。

### 失敗の事象
- `styles.css` の不要記述を削除する際、CSSの閉じカッコ（`}`）などを誤って削除し、構文エラーが発生（パースエラーでレイアウト崩れが発生した）。
- `js/admin.js` での設定UI統合時において、未使用の設定（`config.board`等 が未定義のケース）へのアクセス時に `playwright: TypeError: Cannot read properties of undefined (reading 'includes')` といったエラーメッセージがBrowser Agentで確認された。

### 失敗の根本原因
- CSSの一括置換時に、マルチバイト文字を含むコメントの意図せぬミスマッチに起因する置換範囲の過不足があった。
- 配列メソッド（`includes`）を使用する前に、当該オブジェクトのプロパティ（`board` や `popup`）が存在するかどうかの null チェック（オプショナルチェイニングまたはデフォルト空配列の代入）が欠落していた。

### 次のアプローチ
- 置換ツール使用時は、記号のペア関係やマルチバイト文字の挙動に注意して慎重に範囲指定（単一置換の反復）を行うことを徹底する。
- 外部入力・保存データに依存する動的UI生成時には、常に `|| []` や `?.` などのフェイルセーフな安全策を講じる。
## 2026-03-31: ローカル変更の破棄とリモート同期

### 成功の境界線
- ローカルの変更を完全に破棄し、`origin/dev` に同期する操作（`git fetch && git reset --hard && git clean -fd`）が正常に完了した。
- Git の状態がクリーン（`nothing to commit, working tree clean`）になったことを確認した。

### 失敗の事象と根本原因
- 特になし（意図した通りに動作）

### 次のアプローチ
- 最新の `dev` ブランチの内容を元に、次なる開発タスクあるいは検証を行う。


## 2026-03-27: 現状調査と基本機能の補完

### 成功の境界線
- ログイン画面の表示と拠点情報の取得が正常に行えることを確認。
- ステータス更新（`set` アクション）が D1 データベースに正しく保存され、フロントエンドに反映されることを確認。
- 不足していた `renew`, `getEventColorMap`, `setEventColorMap` アクションの実装が完了。
- データベーススキーマに `event_color_maps` テーブルを追加完了。

### 失敗の事象と根本原因
- 特になし（調査段階で不足を特定し、事前に対処）。

### 実施した検証
1. **ステータス更新検証**:
   - `localStorage` を手動設定してログインをバイパスし、「清水」様のステータスを「外出」に変更。
   - Worker への POST リクエストが `200 OK` を返し、D1 のデータが更新されたことを確認。
   - スクリーンショット [member_status_updated_1774593078673.png] にて表示の正しさを確認。

2. **バックエンド拡張**:
   - `renew`: トークン（拠点・ロール情報）の更新（擬似実装）。
   - `getEventColorMap / setEventColorMap`: 行事カレンダーの日付カラーの永続化を D1 で実装。

### 次のアプローチ
- ローカル環境での詳細な最終検証（特に新設したアクションの動作確認）。
- 本番環境（Cloudflare へのデプロイ）の準備。
- ユーザーへの最終完了報告。

## 2026-03-31 辞書機能の完全削除
- **成功の境界線**: 辞書パネルUIの削除、dictionary.jsの破棄、各フロントエンドスクリプトからの呼び出しの除去まで完了し、コンソールも完全沈黙状態を達成。正常にメンバーの氏名が直接描画されるようになった。
- **失敗の事象**: (エラーなくテスト完了)
- **失敗の根本原因**: なし
- **次のアプローチ**: 本件はクリーンアップとして完了。外字などの置換が必要な場合は、再び別のアプローチ（入力前サニタイズ等）を検討する（今回は完全に手仕舞い）。

## 2026-04-08: QRコード生成エラー（bad rs block）の修正

### 成功の境界線
- ログイン後の「📱 QRcode」表示において、拠点IDを含むURLが正常にQRコード画像（SVG）として生成・表示されるようになった。
- エラー訂正レベルの指定が文字列（'M'など）であっても、内部で正しく数値定数にマッピングされるようになり、ライブラリの参照エラーが解消された。

### 失敗の事象
- QRコード生成時に `QR Generation failed: Error: bad rs block @ typeNumber:1/errorCorrectionLevel:M` というエラーがコンソールに出力され、画像が表示されなかった。

### 失敗の根本原因
- `js/services/qr-generator.js` 内の `getRsBlockTable` 関数が、エラー訂正レベルを数値定数（L=1, M=0...）で厳密比較していたのに対し、`auth.js` からは文字列 `'M'` が渡されていたため、テーブル参照が `undefined` となりクラッシュしていた。
- また、使用していたライブラリの `RS_BLOCK_TABLE` が Version 10 までしか定義されていなかったが、自動判別ロジックが Version 40 まで試行しようとしており、長大なデータ入力時に境界外参照を起こすリスクがあった。

### 次のアプローチ（今回の解決策）
- `qr-generator.js` の `getRsBlockTable` に文字列から数値へのマッピング処理を追加。
- バージョン自動判別（`setTypeNumber(0)`）時のループ範囲を、実際に定義されているテーブル長（Version 10）までに制限し、超過時には適切なエラーを投げるようにガードを追加。

## 2026-04-08: 管理者登録エラー（Firebase設定不備）の修正

### 成功の境界線
- 新規登録（サインアップ）実行時に、Firebase の設定が未完了（プレースホルダの状態）である場合に、親切な日本語エラーメッセージが表示されるようになった。
- ユーザーに `js/firebase-config.js` の設定が必要であることを明示的に伝えられるようになった。

### 失敗の事象
- 管理者登録ボタンを押すと、コンソールに `auth/api-key-not-valid` エラー（400 Bad Request）が発生し、ユーザー画面上には「登録失敗」とだけ表示される不親切な状態だった。

### 失敗の根本原因
- `js/firebase-config.js` 内の `apiKey` 等が `YOUR_API_KEY` というプレースホルダのままであったこと。
- 設定が不完全な状態であることをチェックするガードロジックがフロントエンドに欠落していた。

### 次のアプローチ（今回の解決策）
- `js/auth.js` の登録処理（`btnAuthSignup` のクリックイベント）の冒頭に、`firebaseConfig.apiKey` の値をチェックするバリデーションを追加。
- プレンスホルダを検知した場合は処理を中断し、設定ファイルの編集を促すメッセージを表示するように改善。
- ユーザーより提供された本番用 Firebase 設定情報を `js/firebase-config.js` に反映。
- **追記(2026-04-08 2)**: `CloudflareWorkers_worker.js` のエラーハンドリングを強化。最上位の catch ブロックでテキストではなく JSON を返すように変更し、フロントエンドでのパースエラーを防止。また、`js/auth.js` でオフィスIDを自動小文字化し、大文字混入によるバリデーションエラーを回避する UX 改善を実施。
- **追記(2026-04-08 3)**: `js/auth.js` において、拠点セッション（共有パスワード）が有効な場合に Firebase の未認証チェックが割り込んで UI を「確認メール画面」に強制遷移させる不具合を修正。`watchAuthState` と `switchAuthView` にセッション優先ガードを追加し、既存のログイン状態が非同期な Firebase 通知によって阻害されないように改善。
- **追記(2026-04-08 4)**: 前回の修正がブラウザキャッシュ等の影響で反映されていなかったため、`index.html` にキャッシュバースト用のバージョン文字列 (`v=20260408_v2`) を追加し、`auth.js` 冒頭に実行バージョン確認用のログを追加。ガード条件をさらに厳格化し、ログイン済みのボードが表示されている間は認証系 UI への強制遷移を完全に遮断するように補強。
- **追記(2026-04-08 5)**: ユーザーの指摘に基づき、Worker側の認証失敗が原因でフロントエンドがリロード・再認証ループに陥る可能性を考慮し、`CloudflareWorkers_worker.js` に詳細な認証ログとエラー詳細（reason）を追加。また、フロントエンド `js/auth.js` に `sessionStorage` を利用した「永続セッションロック」を導入。これにより、一度正常にログインすれば、ブラウザを閉じるまで Firebase の状態変化による意図しない画面遷移を強力に防止する実装（Version: v20260408_v3）へとアップデート。
- **追記(2026-04-08 6)**: QRコード（officeパラメータ付きURL）経由でアクセスした際に、未認証のFirebaseセッションが残っていると「確認メール画面」に強制遷移してしまう不具合を修正。`office`パラメータを検知した場合は拠点ログインを優先するガードを `js/auth.js` に追加（Version: v20260408_v5）。また、`index.html` の `verifyEmailArea` に拠点ログイン画面へ戻るための救済リンクを追加。

## 2026-04-14: お知らせ表示不具合の修正（!important 競合の解消）

### 成功の境界線
- お知らせを管理パネルで設定し「表示する」にチェックを入れた際、メイン画面の「お知らせ欄」が即座に、かつ正しく表示されるようになった。
- お知らせボタンをクリックした際、非表示になるのではなく、マニュアル通り「展開/折りたたみ（サマリー表示）」が切り替わるようになった。

### 失敗の事象
- お知らせボタンは表示され、クリックするとトップへスクロールするものの、肝心のお知らせ内容（お知らせ欄）が表示されなかった。

### 失敗の根本原因
- `index.html` の `noticesArea` に `u-hidden` クラス（`display: none !important`）が付与されていた。
- JavaScript 側（`js/notices.js`, `main.js`）で `element.style.display = 'block'` を実行していたが、CSS の `!important` 指定によって上書きが無効化されていた。
- `main.js` 内に `notices.js` と重複する（かつ古い）表示切り替えロジックが残っており、挙動が不安定になっていた。

### 次のアプローチ（今回の解決策）
- `js/notices.js` の `renderNotices` 関数において、お知らせの有無に応じて `classList.remove('u-hidden')` または `classList.add('u-hidden')` を行うように修正。
- `main.js` の冗長な `style.display` 操作を削除し、`notices.js` の `toggleNoticesArea` （`collapsed` クラスの制御）に一本化した。
- インラインスタイルの `display` 操作を最小限に抑え、CSS 設計（SSOT）を優先する実装に変更した。

## 2026-04-14: 管理者登録フローのGUI改善とバリデーション強化

### 成功の境界線
- [x] 管理者登録画面のボタンが「角丸四角（btn-pill）」スタイルに統一され、中央寄せで表示されるようになった。
- [x] パスワードバリデーションが「12文字以上、かつ2種類以上の文字種（大小英字・数字・記号）」という強固な基準に強化された。
- [x] Firebase 認証エラー（例：メールアドレス重複）が適切な日本語メッセージ（「このメールアドレスは既に登録されています」等）に翻訳されて表示されるようになった。
- [x] 認証完了（メール送信後）の画面から不要なリンクを削除し、一貫性のあるUIを実現。
- [x] 新規定数ファイル `js/constants/messages.js` の導入により、UI文言の SSOT（Single Source of Truth）管理を推進。

### 失敗の事象
- 当初、Firebaseエラーが直接表示されており、非専門家には理解しにくい状態だった。
- パスワードの最低条件が「6文字」と緩く、セキュリティ面で不安があった。
- 登録成功後も以前のエラーメッセージが残留してしまうケースがあった。

### 失敗の根本原因
- エラーハンドリングにおいて、Firebase のエラーコードに対するマッピングロジックが欠落していた。
- バリデーションが `auth.js` 内で単純な長さチェックにのみ依存していた。
- ビュー切り替え時にメッセージエリア（`loginMsg`）を明示的にクリアしていなかった。

### 次のアプローチ
- 拠点作成フローにおいても、今回定義した `AUTH_MESSAGES` を活用し、一貫したエラー表示を徹底する。
- パスワード入力欄への「要件のリアルタイムヒント表示」など、UX向上の余地を引き続き検討する。

